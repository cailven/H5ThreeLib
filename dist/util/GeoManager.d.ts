export declare class GeoManager {
    constructor();
    private scene;
    private camera;
    private renderer;
    createCube(_width?: number, _hight?: number, _depth?: number, pos?: THREE.Vector3): THREE.Mesh;
}
